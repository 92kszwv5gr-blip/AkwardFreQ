# Offline tooling only — none of this runs inside the plugin. See tools/README.md.
torch>=2.1
demucs>=4.0.1
onnx>=1.15
onnxscript>=0.1   # needed by torch>=2.5's ONNX exporter internals, even with dynamo=False
scikit-learn>=1.3
skl2onnx>=1.16
numpy>=1.24
